requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
PyYAML>=6.0.1
ruamel.yaml>=0.18.0  # comment-preserving config.yaml writes (scheduler menu); falls back to PyYAML if absent
python-dotenv>=1.0.1
APScheduler>=3.10.4
Flask>=3.0.0
openai>=1.30.0
tenacity>=8.3.0
python-dateutil
curl_cffi>=0.7
# Optional — only needed if you use a Gemini slot (empty LLM_API_URL_N):
# google-genai>=0.3.0
